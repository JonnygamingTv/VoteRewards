name: Build VoteRewards Plugin

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest  # Required for .NET Framework 4.8

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Setup NuGet
        uses: NuGet/setup-nuget@v2

      - name: Restore NuGet packages
        run: nuget restore VoteRewards.csproj

      - name: Build
        run: |
          msbuild VoteRewards.csproj `
            /p:Configuration=Release `
            /p:Platform="AnyCPU" `
            /p:TargetFrameworkVersion=v4.8

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: VoteRewards-Plugin
          path: |
            bin/Release/VoteRewards.dll
            bin/Release/VoteRewards.xml
          if-no-files-found: error

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ github.run_number }}
          files: bin/Release/VoteRewards.dll
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
